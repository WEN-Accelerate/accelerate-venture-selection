import React from 'react'
import ReactDOM from 'react-dom/client'
import ConsultantOnboarding from './ConsultantOnboarding.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ConsultantOnboarding />
    </React.StrictMode>,
)
